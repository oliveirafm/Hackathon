@model IEnumerable<Hackathon.Models.InvoicePaymentDivision>

@{

}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ContractedServiceConfiguration.ExchangeServiceParameter1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Invoice.PaymentService)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Value)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ContractedServiceConfiguration.ExchangeServiceParameter1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Invoice.PaymentService)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Value)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.InvoicePaymentDivisionId }) |
                @Html.ActionLink("Details", "Details", new { id=item.InvoicePaymentDivisionId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.InvoicePaymentDivisionId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
